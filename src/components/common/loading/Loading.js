import styles from "./Loading.module.css";

function Loading() {
    return (
        <div className={styles.container}>
            <div className={styles.textBox}>Loading...</div>
        </div>
    );
}

export default Loading;
